package grimpan.strategy;

import grimpan.strategy.interfaces.PerformMouseDraggedStrategy;
import javafx.scene.input.MouseEvent;

public class LoadingPerformMouseDraggedStrategy implements PerformMouseDraggedStrategy {

    public LoadingPerformMouseDraggedStrategy(){

    }

    @Override
    public void performMouseDragged(MouseEvent event) {

    }
}
